case "$(git symbolic-ref HEAD)":"$(git show HEAD -s --format=%p)" in
refs/heads/master:*\ *)
	VERSION="$(git log -1 --pretty=format:%s HEAD^2 |
		sed -n "s/^[^0-9]*\([\\.0-9]*.\),.*$/\1/p")"
	test -z "$VERSION" ||
	git commit --amend -m "Sync with ImageJ $VERSION"
	;;
esac
