// Use the getNumber() and getString() to get user input.

  n = getNumber("Enter a number:", 100);

  s = getString("Enter a string:", "This is the default string");

// The macro is aborted if the user presses 'Cancel".

// The Dialog.* functions allow entry of numbers and
// strings in a single dialog.
